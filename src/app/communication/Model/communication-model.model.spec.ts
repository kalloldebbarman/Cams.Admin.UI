import { CommunicationModel } from './communication-model.model';

describe('CommunicationModel', () => {
  it('should create an instance', () => {
    expect(new CommunicationModel()).toBeTruthy();
  });
});
