import { JobConfigurationModel } from './job-configuration-model.model';

describe('JobConfigurationModel', () => {
  it('should create an instance', () => {
    expect(new JobConfigurationModel()).toBeTruthy();
  });
});
